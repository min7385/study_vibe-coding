# 카테고리 설정 기능 — 클로드 코드 단계별 프롬프트

`PRD_카테고리설정기능.md`를 기반으로 클로드 코드에서 순서대로 실행할 5단계 프롬프트입니다.
기존 앱(캘린더, 할 일 CRUD, 완료 체크, localStorage) 코드는 유지한 채 이어서 작업합니다.

---

## 1단계: 카테고리 데이터 모델 및 기본값 마이그레이션

**목표**: 카테고리를 별도 데이터로 분리하고, 기존 사용자 데이터와 호환되도록 초기화한다.

```
지금 프로젝트에 카테고리 관리 기능을 추가할 거야. 먼저 데이터 모델부터 준비해줘.

요구사항:
- localStorage key "categories"에 카테고리 목록을 저장한다.
- 카테고리 데이터 구조:
  { id: string, name: string, color: string }
- 앱이 처음 로드될 때 localStorage에 "categories" 키가 없으면
  아래 기본값으로 자동 생성해줘:
  [
    { id: "cat-1", name: "개인", color: "#4A90D9" },
    { id: "cat-2", name: "자기개발", color: "#5CB85C" },
    { id: "cat-3", name: "교회", color: "#9B59B6" },
    { id: "cat-default", name: "미분류", color: "#95A5A6" }
  ]
- "미분류" 카테고리는 시스템 기본 카테고리로, 이후 단계에서 삭제 불가 처리할 예정이니
  id를 "cat-default"로 고정해서 구분할 수 있게 해줘.
- 색상 팔레트 10개를 상수(CATEGORY_PALETTE)로 정의해줘:
  ["#4A90D9", "#5CB85C", "#9B59B6", "#E67E22", "#E74C3C",
   "#F1C40F", "#1ABC9C", "#34495E", "#EC407A", "#95A5A6"]

기존 할 일(todos) 데이터나 UI는 이번 단계에서 건드리지 마.
카테고리 데이터를 저장/조회하는 함수(getCategories, saveCategories 등)만 준비해줘.
```

**완료 기준**: localStorage에 `categories` 키가 정상 생성되고, 기본 4개(미분류 포함) 카테고리 데이터가 콘솔 등으로 확인됨.

---

## 2단계: "카테고리 설정" 버튼 및 팝업(모달) UI 뼈대 구현

**목표**: 할 일 추가 버튼 옆에 버튼을 배치하고, 클릭 시 뜨는 모달의 뼈대를 만든다.

```
할 일 추가 버튼 근처에 "카테고리 설정" 버튼을 추가해줘.

요구사항:
- 버튼 텍스트: "카테고리 설정"
- 위치: 기존 "할 일 추가" 버튼과 같은 줄 또는 바로 옆
- 버튼 클릭 시 화면 중앙에 모달(팝업)이 뜨도록 구현
  - 모달 바깥의 어두운 배경(오버레이) 클릭 시 닫힘
  - 모달 우측 상단에 닫기(X) 버튼
- 모달 내부 구조(아직 기능 없이 레이아웃만):
  - 상단: "카테고리 설정" 제목
  - 중단: 현재 카테고리 목록이 들어갈 영역 (id="category-list")
  - 하단: 새 카테고리 추가 입력 폼이 들어갈 영역 (id="category-add-form")
- 프레임워크나 외부 모달 라이브러리 없이 순수 HTML/CSS/JS로 구현

이번 단계에서는 실제 카테고리 데이터를 렌더링하거나 추가/수정/삭제 로직은 구현하지 마.
버튼-모달 열기/닫기 동작과 레이아웃만 완성해줘.
```

**완료 기준**: "카테고리 설정" 버튼 클릭 시 모달이 뜨고, 오버레이 클릭이나 X 버튼으로 닫힘.

---

## 3단계: 카테고리 목록 표시 및 추가 기능 구현

**목표**: 1단계 데이터와 2단계 UI를 연결해 목록 표시 및 추가 기능을 완성한다.

```
2단계에서 만든 모달에 1단계에서 준비한 카테고리 데이터를 연동해줘.

요구사항:
- 모달을 열 때마다 #category-list 영역에 현재 카테고리 목록을 렌더링
  - 각 항목: 색상 칩(컬러 미리보기) + 이름 + [수정] 버튼 + [삭제] 버튼
  - "미분류" 카테고리는 [삭제] 버튼을 비활성화하거나 숨김 처리
- #category-add-form 영역에 새 카테고리 추가 폼 구현
  - 이름 입력창 (필수)
  - CATEGORY_PALETTE의 10개 색상을 클릭 가능한 색상 원(circle) 형태로 나열, 클릭 시 선택 표시
  - "추가하기" 버튼
- 추가 로직:
  - 이름이 비어있으면 추가 불가 (안내 메시지 표시)
  - 이미 존재하는 이름과 중복되면 추가 불가 (안내 메시지 표시)
  - 정상 추가 시 새 카테고리를 categories 배열에 추가하고 localStorage에 저장, 목록 즉시 갱신

이번 단계에서는 수정/삭제 버튼은 UI만 만들고 클릭해도 동작하지 않아도 돼.
다음 단계에서 구현할 예정이야.
```

**완료 기준**: 모달을 열면 현재 카테고리 목록이 보이고, 새 카테고리를 이름+색상으로 추가하면 목록에 즉시 반영되고 새로고침해도 유지됨.

---

## 4단계: 카테고리 수정 및 삭제(미분류 자동 이동) 기능 구현

**목표**: 카테고리 수정/삭제와, 삭제 시 관련 할 일을 "미분류"로 이동하는 로직을 완성한다.

```
카테고리 목록의 [수정], [삭제] 버튼을 동작하게 만들어줘.

수정 기능:
- [수정] 버튼 클릭 시 해당 항목이 이름 입력창 + 색상 팔레트 선택 UI로 전환
- 저장 시:
  - 이름이 변경되었다면, categories 배열의 해당 항목 업데이트
  - 기존 todos 데이터 중 이전 카테고리 이름을 사용하던 항목들을 찾아서
    category 값을 새 이름으로 일괄 변경 (참조 무결성 유지)
  - localStorage의 "categories"와 "todos" 둘 다 갱신

삭제 기능:
- "미분류" 카테고리(id: "cat-default")는 삭제 버튼이 비활성화되어 있어야 함
- 그 외 카테고리의 [삭제] 버튼 클릭 시:
  - confirm 창으로 "이 카테고리를 사용하는 할 일은 '미분류'로 이동됩니다. 삭제하시겠습니까?" 확인
  - 확인 시: categories 배열에서 해당 카테고리 제거
  - 해당 카테고리를 사용하던 todos 항목들의 category 값을 "미분류"로 일괄 변경
  - localStorage의 "categories"와 "todos" 둘 다 갱신
  - 카테고리 목록 UI 즉시 갱신

수정/삭제 후에는 모달을 닫아도 되고 계속 열려있어도 되니, 사용성이 자연스러운 쪽으로 구현해줘.
```

**완료 기준**: 카테고리 이름 변경 시 관련 할 일들의 카테고리명도 함께 바뀌고, 카테고리 삭제 시 관련 할 일들이 "미분류"로 이동하며 미분류 자체는 삭제되지 않음.

---

## 5단계: 앱 전체 반영 및 최종 통합 테스트

**목표**: 변경된 카테고리 정보가 앱의 모든 영역에 실시간으로 반영되도록 통합하고 전체 기능을 점검한다.

```
카테고리 데이터 변경(추가/수정/삭제)이 앱의 다른 모든 영역에도 즉시 반영되도록 통합해줘.

반영되어야 할 영역:
- 할 일 추가/수정 폼의 카테고리 선택 목록 (드롭다운 등) — 최신 카테고리 목록과 색상으로 갱신
- 오른쪽 상세 영역의 카테고리 필터 목록 — 삭제된 카테고리는 필터에서도 제거, 새 카테고리는 필터에 추가
- 할 일 항목에 표시되는 카테고리 색상 라벨 — categories 데이터의 최신 color 값 참조
- 캘린더의 날짜별 카테고리 점(dot) 색상 — 마찬가지로 최신 color 값 참조

구현 방식은 카테고리 데이터가 바뀔 때마다 관련 UI를 다시 렌더링하는 함수를 호출하는 형태로
일관되게 정리해줘 (예: renderCategorySelectors(), renderCalendar(), renderTodoList() 등
기존 렌더링 함수들을 재사용).

마지막으로 전체 기능을 점검해줘:
- 카테고리 추가 → 할 일 추가 폼에 새 카테고리가 바로 보이는지
- 카테고리 이름 수정 → 기존 할 일의 카테고리 라벨과 캘린더 점 색상도 함께 바뀌는지
- 카테고리 삭제 → 관련 할 일이 "미분류"로 이동하고, 필터/폼에서도 삭제된 카테고리가 사라지는지
- "미분류" 카테고리는 삭제할 수 없는지
- 새로고침 후에도 카테고리와 할 일 데이터가 모두 유지되는지

점검하면서 중복된 렌더링 로직이 있으면 정리하고, 변수/함수명을 기존 코드와 일관되게 다듬어줘.
```

**완료 기준**: 카테고리를 추가/수정/삭제할 때마다 할 일 폼, 필터, 목록, 캘린더가 모두 즉시 갱신되고, 새로고침 후에도 정상 유지됨.

---

## 사용 팁

- 각 단계 프롬프트 끝에 "기존 코드는 유지하고 이어서 작업해줘"를 덧붙이면 이전 단계 작업물이 갈아엎히는 것을 방지할 수 있습니다.
- 3~4단계는 서로 연결되어 있으니 한 세션에서 이어서 진행하는 것을 추천합니다.
- 5단계 완료 후에는 이전에 안내드린 방식대로 `git add . && git commit -m "feat: 카테고리 설정 기능 추가" && git push`로 깃허브에 반영하시면 됩니다.
