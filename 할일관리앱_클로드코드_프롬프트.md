# 할 일 관리 앱 — 클로드 코드 단계별 프롬프트

PRD(`할일관리앱_PRD.md`)를 기반으로 클로드 코드에서 순서대로 실행할 5단계 프롬프트입니다.
각 단계는 이전 단계 결과물 위에 이어서 작업하도록 구성되어 있습니다. 프롬프트를 그대로 복사해서 사용하세요.

---

## 1단계: 프로젝트 구조 및 기본 레이아웃 구성

**목표**: 파일 구조를 잡고, 좌우 2단 레이아웃의 뼈대를 만든다.

```
할 일 관리 웹앱의 기본 뼈대를 만들어줘.

기술 스택: 순수 HTML/CSS/JavaScript (프레임워크, 빌드 도구 없음)
파일 구조: index.html, style.css, script.js 3개 파일로 분리

레이아웃 요구사항:
- 화면을 좌우 2단으로 분할
  - 왼쪽 영역: 캘린더가 들어갈 자리 (id="calendar-area")
  - 오른쪽 영역: 선택한 날짜의 할 일 상세가 들어갈 자리 (id="detail-area")
- 헤더에 앱 제목 표시
- 전체적으로 깔끔하고 여백이 있는 디자인 (아직 세부 스타일은 최소한으로)
- 반응형은 고려하지 않아도 됨 (데스크톱 기준)

이번 단계에서는 실제 캘린더 로직이나 할 일 기능은 구현하지 말고,
레이아웃 구조와 최소한의 스타일만 완성해줘.
```

**완료 기준**: 브라우저로 열었을 때 좌/우 2단 레이아웃이 보이고, 각 영역의 자리가 명확히 구분됨.

---

## 2단계: 캘린더 기능 구현

**목표**: 왼쪽 영역에 동작하는 월간 캘린더를 만든다.

```
1단계에서 만든 레이아웃의 왼쪽 영역(#calendar-area)에 월간 캘린더를 구현해줘.

요구사항:
- 현재 월을 기본으로 표시
- 상단에 "이전 달 / 현재 연월 / 다음 달" 이동 버튼
- 요일(일~토) 헤더와 날짜 그리드 표시
- 오늘 날짜는 시각적으로 하이라이트
- 날짜를 클릭하면 해당 날짜가 "선택된 날짜"로 표시됨 (선택된 날짜도 시각적으로 구분)
- 날짜 클릭 시 선택된 날짜 정보를 콘솔에 로그로 출력 (다음 단계에서 상세 영역과 연동 예정)
- 날짜는 YYYY-MM-DD 형식의 문자열로 다룰 것 (이후 데이터 모델과 맞추기 위함)

아직 할 일 데이터나 오른쪽 상세 영역과의 연동은 구현하지 마.
캘린더 자체의 동작(월 이동, 날짜 선택)만 완성해줘.
```

**완료 기준**: 월 이동이 정상 동작하고, 날짜 클릭 시 선택 상태가 시각적으로 바뀜.

---

## 3단계: 할 일 데이터 모델 및 CRUD 기능 구현

**목표**: 오른쪽 상세 영역에서 할 일을 추가/수정/삭제할 수 있게 한다.

```
이제 오른쪽 영역(#detail-area)에 할 일 CRUD 기능을 구현해줘.

데이터 모델은 다음 구조를 따라줘:
{
  id: string,          // 고유 id (timestamp 등으로 생성)
  title: string,        // 할 일 제목
  category: string,      // "개인" | "공부" | "교회"
  date: string,          // "YYYY-MM-DD"
  memo: string,          // 선택 입력
  completed: boolean,    // 완료 여부
  createdAt: string       // 생성 시각
}

요구사항:
- 캘린더에서 선택한 날짜를 상단에 표시 (예: "2026년 7월 30일 목요일")
- "할 일 추가" 버튼을 누르면 제목/카테고리/메모 입력 폼이 나타남
  - 제목은 필수, 카테고리는 select 등으로 선택(개인/자기개발/교회 3종), 메모는 선택 입력
  - 저장 시 선택된 날짜를 자동으로 date 값으로 사용
- 추가된 할 일은 선택된 날짜 아래에 목록으로 표시
- 각 할 일 항목에 "수정", "삭제" 버튼 제공
  - 수정: 클릭 시 기존 값이 채워진 입력 폼으로 전환, 저장하면 반영
  - 삭제: 클릭 시 목록에서 즉시 제거 (확인창 없이 바로 삭제해도 됨)
- 이 단계에서는 데이터를 자바스크립트 메모리(배열 변수)에만 저장해도 됨.
  localStorage 연동은 다음 단계에서 진행할 예정이야.

캘린더 쪽 코드는 2단계 결과를 그대로 유지하고, 날짜 선택 시
오른쪽 상세 영역이 해당 날짜 데이터로 갱신되도록 연동해줘.
```

**완료 기준**: 날짜 선택 → 할 일 추가/수정/삭제가 오른쪽 영역에서 정상 동작.

---

## 4단계: 완료 체크 및 카테고리 시각화

**목표**: 완료 처리와 카테고리 구분을 시각적으로 명확하게 만든다.

```
할 일 목록에 완료 체크 기능과 카테고리 시각화를 추가해줘.

요구사항:
- 각 할 일 항목 왼쪽에 체크박스를 추가하고, 클릭 시 completed 값을 토글
- 완료된 항목은 텍스트에 취소선을 적용하고, 살짝 흐리게(opacity) 표시
- 카테고리별로 색상을 구분해줘 (예: 개인=파란색 계열, 자기개발=초록색 계열, 교회=보라색 계열)
  - 할 일 항목에 카테고리 색상 라벨(또는 좌측 컬러 바) 표시
- 캘린더 쪽에도 해당 날짜에 할 일이 하나 이상 있으면 작은 점(dot) 표시를 추가해줘
  - 여러 카테고리가 섞여 있으면 점을 여러 개 또는 대표 색상 하나로 표시 (구현하기 쉬운 방식으로 선택)
- 오른쪽 상세 영역 상단에 카테고리 필터(전체/개인/자기개발/교회)를 추가해서
  선택한 카테고리의 할 일만 보이도록 필터링 기능도 추가해줘
```

**완료 기준**: 완료 체크 시 취소선 적용, 카테고리별 색상 구분, 캘린더에 할 일 표시 점이 보임.

---

## 5단계: localStorage 연동 및 최종 통합

**목표**: 데이터를 영구 저장하고 전체 기능을 통합 테스트한다.

```
지금까지 메모리에만 저장하던 할 일 데이터를 localStorage와 연동해줘.

요구사항:
- localStorage key는 "todos"로 통일
- 앱이 처음 로드될 때 localStorage에서 데이터를 불러와 화면에 반영
  (데이터가 없으면 빈 배열로 시작)
- 할 일을 추가/수정/삭제/완료토글 할 때마다 localStorage에 즉시 저장
- JSON.parse/JSON.stringify 사용 시 예외 처리(try/catch)를 추가해서
  localStorage 데이터가 손상되었거나 없을 경우에도 앱이 깨지지 않도록 해줘

마지막으로 전체 기능을 점검해줘:
- 캘린더 월 이동 및 날짜 선택
- 할 일 추가/수정/삭제
- 완료 체크 토글
- 카테고리 필터 및 색상 표시
- 새로고침 후에도 데이터가 유지되는지

전체 코드를 검토하면서 불필요하게 중복된 로직이 있으면 정리하고,
변수명과 함수명을 일관되게 다듬어줘.
```

**완료 기준**: 새로고침해도 모든 할 일 데이터가 그대로 유지되고, 전체 기능이 오류 없이 동작함.

---

## 사용 팁

- 각 단계는 순서대로 진행하되, 이전 단계 결과를 클로드 코드가 참고하도록 같은 세션/같은 저장소에서 이어서 요청하세요.
- 각 프롬프트 끝에 "이전 단계 코드는 유지하고 이어서 작업해줘"라는 문구를 덧붙이면 기존 코드를 갈아엎는 것을 방지할 수 있습니다.
- 중간에 원하는 디자인(색상, 폰트 등)이 있다면 4단계 전에 별도로 스타일 다듬기 프롬프트를 추가해도 좋습니다.
