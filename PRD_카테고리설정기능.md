# 할 일 관리 앱 — 카테고리 커스터마이징 기능 PRD

> 본 문서는 기존 `할일관리앱_PRD.md`의 확장 기능 명세입니다. 기존 앱 구조와 기본 기능(캘린더, 할 일 CRUD, 완료 체크, localStorage 저장)은 그대로 유지되며, 이 문서는 **카테고리 관리 기능 추가**에 대해서만 다룹니다.

## 1. 현재 상태 요약

- 현재 카테고리는 `개인`, `자기개발`, `교회` 3종으로 코드에 고정되어 있음
- 할 일 데이터 구조: `{ id, title, category, date, memo, completed, createdAt }`
- 카테고리는 색상으로 구분되어 할 일 항목과 캘린더 점(dot) 표시에 사용 중

## 2. 배경 및 목적

- 현재는 카테고리가 3종으로 고정되어 있어 사용자가 필요에 따라 항목을 추가/변경할 수 없음
- 사용 패턴이 바뀌거나 새로운 영역(예: "운동", "가족")이 생기면 유연하게 대응할 수 있어야 함
- 카테고리를 사용자가 직접 관리(추가/수정/삭제)할 수 있는 설정 기능이 필요

## 3. 핵심 사용자 시나리오

1. 사용자가 할 일 추가 영역 근처에서 "카테고리 설정" 버튼을 발견한다.
2. 버튼을 클릭하면 카테고리 관리 팝업(모달)이 뜬다.
3. 팝업에서 기존 카테고리 목록(이름 + 색상)을 확인한다.
4. "추가" 버튼으로 새 카테고리(이름, 색상)를 등록한다.
5. 기존 카테고리의 이름이나 색상을 수정한다.
6. 더 이상 필요 없는 카테고리를 삭제한다. (단, 해당 카테고리를 사용 중인 할 일이 있을 경우 처리 방식 정의 필요 — 5.4 참고)
7. 팝업을 닫으면 변경된 카테고리 목록이 할 일 추가 폼의 선택지, 필터, 캘린더 색상 표시 등 앱 전체에 즉시 반영된다.

## 4. 기능 요구사항

### 4.1 UI 배치
- 할 일 추가 버튼과 가까운 위치(예: 같은 줄 또는 바로 옆)에 **"카테고리 설정"** 텍스트가 적힌 버튼을 배치
- 버튼 클릭 시 화면 중앙에 모달(팝업) 형태로 카테고리 관리 UI가 뜸
- 모달 바깥 영역 클릭 또는 닫기(X) 버튼으로 팝업 닫기 가능

### 4.2 카테고리 목록 조회
- 팝업 내에 현재 등록된 카테고리 목록을 표시
- 각 항목마다: 카테고리 이름, 색상 미리보기(컬러 칩), 수정 버튼, 삭제 버튼

### 4.3 카테고리 추가
- 팝업 내 "카테고리 추가" 입력 영역: 이름 입력(필수), 색상은 **사전 정의된 10개 팔레트 중 클릭으로 선택** (자유 색상 입력 없음)
- 이름 중복 등록 방지 (동일 이름 카테고리 존재 시 안내 메시지)
- 추가 완료 시 목록에 즉시 반영

### 4.4 카테고리 수정
- 목록에서 "수정" 버튼 클릭 시 해당 항목이 편집 가능한 입력 상태로 전환
  - 이름: 텍스트 수정 가능
  - 색상: 동일한 10개 팔레트 중 재선택 가능
- 이름을 변경할 경우, 해당 카테고리를 사용 중이던 기존 할 일 데이터의 `category` 값도 함께 업데이트 (참조 무결성 유지)

### 4.5 카테고리 삭제
- 목록에서 "삭제" 버튼 클릭 시 확인창(confirm) 표시 후 삭제
- **삭제 정책 (확정)**: 해당 카테고리를 사용 중인 할 일이 있을 경우, 삭제와 동시에 해당 할 일들의 카테고리는 자동으로 **"미분류"**로 이동
  - 별도의 확인창(confirm)은 유지하되, "미분류로 이동됩니다" 안내 문구를 포함
- "미분류"는 시스템 기본 카테고리로 항상 존재하며 **삭제 불가** 처리
- 카테고리가 최소 1개(미분류 포함)는 남아있도록 강제

### 4.6 데이터 반영 범위
카테고리 변경 시 다음 UI 요소들이 즉시 갱신되어야 함:
- 할 일 추가/수정 폼의 카테고리 선택 목록
- 오른쪽 상세 영역의 카테고리 필터 목록
- 할 일 항목의 색상 라벨
- 캘린더의 날짜별 카테고리 점(dot) 색상

## 5. 데이터 모델 변경

### 5.1 신규: 카테고리 데이터 구조
```javascript
// localStorage key: "categories"
[
  { id: "cat-1", name: "개인", color: "#4A90D9" },
  { id: "cat-2", name: "자기개발", color: "#5CB85C" },
  { id: "cat-3", name: "교회", color: "#9B59B6" }
]
```
- 앱 최초 실행 시 위 3종을 기본값으로 자동 생성 (기존 사용자 데이터 호환)

### 5.2 기존 할 일 데이터 구조 변경
- 기존: `category: "개인"` (문자열 이름 직접 저장)
- 변경 옵션 두 가지 중 선택:
  - **옵션 A (간단, 권장)**: 계속 카테고리 **이름**을 문자열로 저장하고, 이름이 바뀌면 관련 할 일들의 문자열도 일괄 변경
  - **옵션 B (정규화)**: `categoryId`로 참조하도록 변경 (더 견고하지만 기존 데이터 마이그레이션 필요)
- → 이번 버전은 기존 코드 변경을 최소화하기 위해 **옵션 A**로 진행 권장

### 5.3 마이그레이션 처리
- 앱 로드 시 `localStorage`에 `categories` 키가 없다면, 하드코딩되어 있던 기존 3종 카테고리 정보로 자동 생성
- 기존에 저장된 할 일(`todos`)의 category 값은 그대로 유지 (이름 문자열이 동일하므로 별도 변환 불필요)

### 5.4 삭제 시 처리 (4.5의 옵션 A 적용 시)
- "미분류" 카테고리를 기본 카테고리로 항상 유지 (삭제 불가)
- 사용자가 카테고리 삭제 시, 해당 카테고리를 쓰던 할 일들의 `category` 값을 `"미분류"`로 일괄 변경

## 5.5 색상 팔레트 정의 (예시)

10개 고정 팔레트를 상수로 정의하여 색상 선택 UI에 사용:

```javascript
const CATEGORY_PALETTE = [
  "#4A90D9", // 파랑
  "#5CB85C", // 초록
  "#9B59B6", // 보라
  "#E67E22", // 주황
  "#E74C3C", // 빨강
  "#F1C40F", // 노랑
  "#1ABC9C", // 청록
  "#34495E", // 남색
  "#EC407A", // 핑크
  "#95A5A6"  // 회색 ("미분류" 기본 색상으로 사용)
];
```

## 6. 화면 구성 (와이어프레임 설명)

```
┌───────────────────────────────────────────┐
│  2026년 7월 30일 (목)                       │
│  ─────────────────────────────────────    │
│  [+ 할 일 추가]  [⚙ 카테고리 설정]           │
│                                             │
│  ☐ 개인   운동하기            [수정][삭제]  │
│  ☑ 자기개발 책 읽기            [수정][삭제]  │
└───────────────────────────────────────────┘

카테고리 설정 버튼 클릭 시:

        ┌─────────────────────────────┐
        │   카테고리 설정          [X] │
        │ ───────────────────────────  │
        │  ● 개인        [수정][삭제]  │
        │  ● 자기개발     [수정][삭제]  │
        │  ● 교회        [수정][삭제]  │
        │ ───────────────────────────  │
        │  + 새 카테고리 추가            │
        │  이름: [________]             │
        │  색상: ●●●●●●●●●● (10개 중 선택)│
        │  [추가하기]                   │
        └─────────────────────────────┘
```

## 7. 비기능 요구사항

- 기존 기능(캘린더, 할 일 CRUD, 완료 체크, localStorage 저장)에 영향을 주지 않아야 함
- 프레임워크 없이 순수 JS로 모달 구현 (별도 라이브러리 사용 안 함)
- 카테고리가 0개가 되는 상황은 방지 (최소 1개 이상 유지)

## 8. 개발 범위 (이번 기능 기준)

| 우선순위 | 기능 |
|---|---|
| 필수 | 카테고리 설정 버튼, 팝업 모달, 카테고리 추가/수정/삭제, 10개 색상 팔레트 선택 UI, 삭제 시 "미분류" 자동 이동, 데이터 전 영역 즉시 반영, localStorage(`categories`) 연동 |
| 제외 | 자유 색상 코드 입력, 카테고리 순서 드래그 정렬, 카테고리별 아이콘 설정 |

## 9. 확정 사항

1. **카테고리 삭제 정책**: 삭제 시 해당 카테고리를 사용하던 할 일들은 자동으로 "미분류"로 이동 (경고 문구 포함 확인창 표시)
2. **색상 선택 방식**: 사전 정의된 10개 팔레트 중 클릭으로 선택 (5.5 참고)
