# 기간/시간 등록 기능 — 클로드 코드 단계별 프롬프트

`PRD_기간시간기능.md`를 기반으로 클로드 코드에서 순서대로 실행할 5단계 프롬프트입니다.
기존 앱(캘린더, 할 일 CRUD, 완료 체크, 카테고리 관리, localStorage) 코드는 유지한 채 이어서 작업합니다.

---

## 1단계: 데이터 모델 변경 및 기존 데이터 마이그레이션

**목표**: 할 일 데이터 구조를 `date` 단일 필드에서 `startDate/endDate/startTime/endTime` 구조로 변경하고, 기존 데이터를 자동 변환한다.

```
할 일 데이터 구조를 변경할 거야. 기존에는 date 하나로 하루 단위 할 일만 관리했는데,
이제 기간(시작일~종료일)과 시간(시작시간~종료시간)을 다룰 수 있도록 바꿔줘.

변경 후 데이터 구조:
{
  id: string,
  title: string,
  category: string,
  startDate: string,   // "YYYY-MM-DD" — 기존 date를 대체
  endDate: string,      // "YYYY-MM-DD" — 신규, 하루짜리는 startDate와 동일
  startTime: string | null,   // "HH:MM" 형식, 없으면 null (종일 일정)
  endTime: string | null,     // "HH:MM" 형식, 없으면 null
  memo: string,
  completed: boolean,
  createdAt: string
}

마이그레이션 요구사항:
- 앱 로드 시 localStorage의 "todos"를 불러올 때, 기존 date 필드만 있고
  startDate가 없는 항목을 발견하면 자동으로 변환:
  - startDate = date, endDate = date
  - startTime = null, endTime = null
- 변환이 끝나면 새 구조로 localStorage에 다시 저장
- 이미 새 구조로 저장된 데이터는 변환하지 않고 그대로 사용 (중복 변환 방지)

이번 단계에서는 UI는 건드리지 말고, 데이터 구조와 마이그레이션 로직만 완성해줘.
마이그레이션이 잘 되는지 콘솔 로그로 확인할 수 있게 해줘.
```

**완료 기준**: 기존에 저장되어 있던 할 일 데이터가 새 구조(`startDate`, `endDate`, `startTime`, `endTime`)로 자동 변환되어 저장됨.

---

## 2단계: 할 일 추가/수정 폼에 기간·시간 입력 UI 추가

**목표**: 기존 날짜 입력 하나였던 폼을 시작일/종료일, 시작시간/종료시간 입력으로 확장한다.

```
할 일 추가/수정 폼을 수정해줘.

변경 사항:
- 기존의 단일 날짜 입력을 "시작일", "종료일" 두 개의 date input으로 교체
- 캘린더에서 선택된 날짜가 있으면, 폼을 열 때 시작일=종료일=선택된 날짜로 기본값 자동 설정
- "시작 시간", "종료 시간" time input을 새로 추가 (둘 다 선택 입력, 비워둘 수 있음)
- 시간 입력란 위나 옆에 "시간을 비워두면 종일 일정으로 등록됩니다" 안내 문구 표시
- 저장 버튼 클릭 시 1단계에서 정의한 데이터 구조(startDate, endDate, startTime, endTime)로
  값을 만들어서 저장하도록 기존 저장 로직을 수정

이번 단계에서는 아직 유효성 검사(종료일이 시작일보다 빠른 경우 등)는 구현하지 마.
입력 폼 UI와 저장 로직 연결까지만 완성해줘.
```

**완료 기준**: 할 일 추가/수정 폼에서 시작일/종료일/시작시간/종료시간을 입력하고 저장하면 새 데이터 구조로 localStorage에 반영됨.

---

## 3단계: 입력값 유효성 검사 구현

**목표**: 기간과 시간 입력에 대한 유효성 검사를 추가해 잘못된 데이터 등록을 방지한다.

```
할 일 추가/수정 폼에 유효성 검사를 추가해줘.

요구사항:
- 종료일이 시작일보다 이전이면 저장을 막고 "종료일은 시작일보다 빠를 수 없습니다" 안내 메시지 표시
- 시작 시간과 종료 시간이 둘 다 입력되어 있고, 시작일과 종료일이 같은 날짜인 경우에 한해:
  - 종료 시간이 시작 시간보다 이전이면 저장을 막고 "종료 시간은 시작 시간보다 빠를 수 없습니다" 안내 메시지 표시
- 시작 시간만 입력하고 종료 시간을 비워둔 경우(또는 그 반대)는 허용 (필수 조합 아님)
- 제목이 비어있는 경우의 기존 유효성 검사 로직은 그대로 유지

유효성 검사에 실패하면 폼이 닫히지 않고 그대로 유지되어 사용자가 수정할 수 있게 해줘.
```

**완료 기준**: 종료일<시작일, 같은 날 종료시간<시작시간 조합을 입력하면 저장이 차단되고 안내 메시지가 뜸.

---

## 4단계: 캘린더에 기간 바(bar) 형태로 표시

**목표**: 여러 날에 걸친 할 일을 캘린더에서 시작일~종료일에 걸쳐 막대 형태로 보여준다.

```
캘린더에서 기간이 여러 날에 걸친 할 일을 시각적으로 표시해줘.

요구사항:
- 각 할 일의 startDate ~ endDate 범위에 해당하는 모든 날짜 셀에 걸쳐
  가로 막대(bar) 형태로 표시 (색상은 해당 할 일의 카테고리 색상 사용)
- 하루짜리 할 일(startDate === endDate)도 동일한 방식으로 짧은 막대 하나로 표시해서
  일관성을 유지해줘
- 한 날짜에 여러 할 일(막대)이 겹치는 경우, 세로로 쌓아서 표시 (겹치지 않게)
- 날짜 셀 안에 표시할 공간이 부족할 경우, 일부만 보여주고 "+N개 더보기" 형태로
  간단히 처리해도 괜찮아 (구현 난이도를 고려해서 판단해줘)
- 막대를 클릭하면 해당 할 일의 상세/수정 화면으로 이동하거나 열리도록 연결

기존 캘린더의 월 이동, 날짜 선택, 오늘 날짜 하이라이트 기능은 그대로 유지하면서
이 기능을 추가해줘.
```

**완료 기준**: 여러 날에 걸친 할 일이 캘린더에서 시작일부터 종료일까지 막대로 이어져 보이고, 겹치는 경우 세로로 구분되어 표시됨.

---

## 5단계: 상세 목록 시간 표시, 날짜별 필터링 로직 통합, 최종 테스트

**목표**: 오른쪽 상세 영역에서 시간 표시와 기간 기반 필터링을 완성하고 전체 기능을 통합 점검한다.

```
오른쪽 상세 영역의 목록 표시 로직을 기간/시간 구조에 맞게 정리해줘.

요구사항:
- 캘린더에서 특정 날짜를 선택하면, 그 날짜가 startDate~endDate 범위 안에 포함되는
  모든 할 일을 상세 목록에 표시 (기존처럼 date === 선택날짜 로만 비교하던 로직을
  범위 비교로 변경)
- 목록에서 시간이 설정된 할 일은 제목 앞에 "시작시간" 표시 (예: "14:00 팀 회의")
- 시간이 없는 종일 일정은 "종일"이라고 표시하고 목록 상단에 노출
- 여러 날에 걸친 할 일은 오늘이 몇 일차인지 표시 (예: "여름휴가 (3일차/5일)")
- 같은 날짜에 여러 할 일이 있을 경우, 시간이 있는 항목은 시간 순으로,
  종일 일정은 그 위나 아래에 일관되게 정렬

마지막으로 전체 기능을 점검해줘:
- 기간(여러 날) 할 일 등록 → 캘린더에 막대로 표시되고, 각 날짜를 선택할 때마다
  상세 목록에도 정상적으로 나타나는지
- 시간 등록 → 상세 목록에 시간이 올바르게 표시되는지
- 유효성 검사(3단계)가 정상 동작하는지
- 기존 하루짜리/카테고리 기능이 이번 변경으로 깨지지 않았는지
- 새로고침 후에도 모든 데이터(기간, 시간 포함)가 유지되는지

점검하면서 중복 로직이 있으면 정리하고, 변수/함수명을 기존 코드와 일관되게 다듬어줘.
```

**완료 기준**: 기간/시간이 캘린더와 상세 목록 양쪽에 정확히 반영되고, 기존 기능들과 충돌 없이 새로고침 후에도 데이터가 유지됨.

---

## 사용 팁

- 각 단계 프롬프트 끝에 "기존 코드는 유지하고 이어서 작업해줘"를 덧붙이면 이전 단계 작업물이 갈아엎히는 것을 방지할 수 있습니다.
- 1단계(마이그레이션)는 특히 중요하니, 실제 저장된 기존 데이터로 마이그레이션이 잘 되는지 꼭 확인 후 다음 단계로 넘어가세요.
- 5단계 완료 후에는 `git add . && git commit -m "feat: 기간 및 시간 등록 기능 추가" && git push`로 깃허브에 반영하시면 됩니다.
