# 할 일 관리 앱 — 기간/시간 등록 기능 PRD

> 본 문서는 기존 `할일관리앱_PRD.md`의 확장 기능 명세입니다. 기존 앱 구조와 기본 기능(캘린더, 할 일 CRUD, 완료 체크, 카테고리 관리, localStorage 저장)은 그대로 유지되며, 이 문서는 **기간 등록 / 시작·종료 시간 등록 기능 추가**에 대해서만 다룹니다.

## 1. 현재 상태 요약

- 현재 할 일은 하루 단위(`date: "YYYY-MM-DD"`)로만 등록 가능
- 시간 개념 없이 날짜에만 귀속되어 있음
- 데이터 구조: `{ id, title, category, date, memo, completed, createdAt }`

## 2. 배경 및 목적

- 하루짜리 할 일뿐 아니라 여러 날에 걸친 일정(예: "여름휴가", "프로젝트 기간")을 등록하고 싶음
- 특정 시간에 시작하는 일정(예: "10시 회의")을 명확히 기록하고 싶음

## 3. 핵심 사용자 시나리오

1. 사용자가 할 일을 추가할 때, 하루가 아니라 "시작일~종료일" 기간으로 등록할 수 있다.
2. 하루짜리 할 일의 경우 시작일=종료일로 두어 간단히 등록한다.
3. 할 일에 시작 시간과 종료 시간을 선택적으로 입력한다 (예: 14:00~15:00).
4. 캘린더에서 기간이 걸쳐 있는 할 일은 여러 날짜에 걸쳐 표시된다.
5. 특정 날짜를 선택하면, 그 날짜에 걸쳐 있는(시작~종료 기간에 포함되는) 모든 할 일이 오른쪽 상세 영역에 표시된다.

## 4. 기능 요구사항

### 4.1 기간 등록
- 할 일 추가/수정 폼에 "시작일"과 "종료일" 두 개의 날짜 입력란 제공
- 캘린더에서 특정 날짜를 선택한 상태로 "할 일 추가"를 누르면, 시작일=종료일=선택한 날짜로 기본값 설정 (기존처럼 하루짜리 등록도 그대로 간편하게 가능)
- 종료일은 시작일보다 이전일 수 없도록 유효성 검사
- 기간이 여러 날에 걸친 할 일은 캘린더 상에서 시작일부터 종료일까지 모든 날짜 셀에 표시(바 형태)

### 4.2 시작/종료 시간 등록
- 할 일 추가/수정 폼에 "시작 시간", "종료 시간" 입력란 추가 (선택 입력, 비워두면 종일 일정으로 처리)
- 시간 입력은 `HH:MM` 형식 (예: 14:00)
- 종료 시간이 시작 시간보다 빠를 수 없도록 유효성 검사 (같은 날짜 기준)
- 오른쪽 상세 목록에서 시간이 설정된 할 일은 제목 앞에 시간 표시 (예: "14:00 팀 회의")
- 시간이 없는 할 일(종일 일정)은 기존처럼 시간 표시 없이 목록 상단에 노출

## 5. 데이터 모델 변경

### 5.1 할 일 데이터 구조 변경
```javascript
// 기존
{ id, title, category, date, memo, completed, createdAt }

// 변경 후
{
  id: "uuid-or-timestamp",
  title: "팀 회의",
  category: "개인",
  startDate: "2026-08-10",   // 기존 date를 startDate로 변경
  endDate: "2026-08-10",     // 신규: 종료일 (하루짜리는 startDate와 동일)
  startTime: "14:00",         // 신규: 선택 입력, 없으면 null
  endTime: "15:00",           // 신규: 선택 입력, 없으면 null
  memo: "",
  completed: false,
  createdAt: "..."
}
```

### 5.2 기존 데이터 마이그레이션
- 앱 로드 시 기존 `date` 필드만 있는 할 일 데이터를 발견하면 자동 변환:
  - `startDate = date`, `endDate = date`
  - `startTime = null`, `endTime = null`
- 변환 후 localStorage에 새 구조로 다시 저장

## 6. 화면 구성 (와이어프레임 설명)

```
할 일 추가/수정 폼:
┌───────────────────────────────┐
│  제목: [________________]      │
│  카테고리: [드롭다운 ▾]         │
│  시작일: [2026-08-10]  종료일: [2026-08-10] │
│  시작 시간: [14:00]  종료 시간: [15:00]    │
│   (시간은 비워두면 종일 일정)              │
│  메모: [________________]      │
│  [저장]                        │
└───────────────────────────────┘

오른쪽 상세 목록:
┌───────────────────────────────┐
│ 2026년 8월 10일 (월)            │
│ ───────────────────────────    │
│ ☐ 14:00 팀 회의        [수정][삭제] │
│ ☐ 종일   여름휴가 (3일차/5일)  [수정][삭제] │
└───────────────────────────────┘
```

## 7. 비기능 요구사항

- 기존 기능(캘린더, CRUD, 완료 체크, 카테고리 관리)에 영향을 주지 않아야 함
- 서버, 빌드 도구, 외부 라이브러리 없이 순수 JS로 구현

## 8. 개발 범위 (이번 기능 기준)

| 우선순위 | 기능 |
|---|---|
| 필수 | 시작일/종료일 등록, 캘린더 기간 표시(바 형태), 시작/종료 시간 등록, 시간 유효성 검사, 기존 데이터 마이그레이션 |
| 제외 | 반복 일정 |

## 9. 확정이 필요한 질문

없음 — 이번 기능 범위는 모두 확정되었습니다.
